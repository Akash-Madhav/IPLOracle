services:
  - type: web
    name: ipl-insight-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
    envVars:
      - key: GEMINI_API_KEY
        sync: false